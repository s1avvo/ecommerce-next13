query ProductGetList($limit: Int!, $offset: Int!) {
	productsConnection(first: $limit, skip: $offset) {
		products: edges {
			node {
				...ProductListItem
			}
		}
		pageInfo {
			hasNextPage
			hasPreviousPage
		}
	}
}
